1. 进行项目的浏览理解

1. 现在，小说的文章编辑部分出了问题，我的输入内容输入不进去，仔细检查这种项目类型的文本编辑器是否有bug

1. 检查快捷键，每一个快捷键我都设置了使用范围，检查在每个情况是否都能正常使用，我现在在首页无法使用老板键和全屏
2. 现在软件的F12进入元素控制台面板怎么打不开，不方便我进行调试啊

1. 我还是无法打开开发者面板

1. 我关闭软件，重新 npx tauri dev 了，但是一些默认的快捷键还是无法使用，F5的刷新，F12的开发者面板还是无法使用

1. 我用F12不行，但是 Ctrl+Shift+I 可以
2. 进入软件有报错 Uncaught (in promise) TypeError: getCurrentWindow(...).openDevtools is not a function
    at useKeyboardShortcuts.ts:125:28

1. 我按F12 出现报错Uncaught (in promise) TypeError: openDevtools is not a function

1. 小说的编辑器还是有问题，居停情况是 我输入内容例如 谢谢等任意内容，编辑器没有写入，但是再次输入内容这个内容就写入了，所有输入的内容都得第2遍才能写入编辑器

1. 小说编辑器的内容区域有大问题！内容输入 不论输入什么，第一次输入都不能显示，这不就是一个文本编辑器吗，怎么那么麻烦，你仔细检查逻辑啊，实在不行给每次输入控制台加个打印，现在没有任何反馈，我都不知道问题在哪，
2. 小说文章内容删除后，编辑企业没变化，我都把章节删完了，他还有内容
3. 我新增的章节 怎么有默认存的之前的文章内容，仔细检查逻辑

1. 现在的情况是，我在内容编辑区键盘输入时，对于任意的内容，初次输入时，没有触发任何打印，二次输入触发打印，第三次输入又不出发，第四次才触发，意思就是，假如我每次只输入一个内容，例如啊，那么第一次输入，没有任何反应，第二次输入，编辑器添加了一个啊，控制台有打印信息，第三次输入没有任何反应，第4次输入，编辑器出现第2个啊，控制台有打印
[Editor] @change fired, doc length: 1 first 20 chars: "啊"
EditorArea.vue:145 [Editor] watch currentContent, length: 1 contentFromEditor: true
EditorArea.vue:117 [Editor] @change fired, doc length: 2 first 20 chars: "啊啊"
EditorArea.vue:145 [Editor] watch currentContent, length: 2 contentFromEditor: true
实际上我输入了四次，但实际只有两个，偶数次的才能加载上
更简单地说，就是奇数次的输入不论任何内容都无法加载，然后偶数次的输入就可以

1. 什么玩意，你在干什么，这不就是一个文本编辑器吗，哪来的什么循环，什么双向反馈循环，这都是在干什么！

1. 我现在不理解你的写法是什么了，这不就是vue的组件吗，双向绑定有那么难吗，就是一个内容的输入与显示，怎么就实现不了呢，而且这个工鞥你之前是完成了的，不知道那次的修改冲突了，现在进行仔细的检查好吧

1.现在，我感觉根本就不是这各组件的问题，我将Codemirror组件的<Codemirror/>相关事件都删除了，只保留原始组件，但是，输入还是奇数次没效果，偶数次正常输入
现在有几个思路
父组件有没有 v-if、v-show 快速切换？
有没有 :key 绑定时间戳 / 随机数？
有没有 keep-alive 嵌套？
这些会有影响吗

1.textarea没问题，那可能就是第三方组件的问题，是不是版本问题

1.现在的确是这个Codemirror组件的问题，下面这是官方文档说明，你思考一下

1.现在小说类型项目的文件操作有问题，我编辑了当前文件，但是切换到其余文件也是显示当前内容，如果删除了别的文件，所有内容都消失，检查相关代码状态管理是不是没做好，文件相互之间没隔离，造成了数据的相互污染
修复，但是注意相关代码之间联系，不要影响到别的功能

1. 现在文章内容自动保存是多少时间

1. 当前小说章节是1.5秒保存，那要是切换，我输入完内容之后，1.5秒以内切换了文件，那内容是怎么保存？是不是有逻辑漏洞？
所以你要做到切换文件时，直接保存，切换完之后更换成打开的文件内容，

1. 现在对目录系统以及用户切换不同章节时，文章内容的切换，以及内容保存，切换机制，保存机制，确保不会冲突，因为现在的问题是，多张内容，我编辑了其中一个内容，  然后切换文件，但现实的还是上一个文件内容，就是内容编辑区域的显示没有随之切换 

1. 不对不对，还是不能正常切换显示，不就是一个文本编辑器吗，实在不行你也每次切换文件重新加载不行吗，现在全是bug

1.你有没有考虑过，你删掉的那些标志位逻辑是干什么的删掉了会对别的功能有影响吗，而且，你这一个编辑企鹅的切换逻辑怎么就整不明白了，不明白你就上网搜，看别人的文件切换，以及编辑器是如何实现的

1.现在这样不就好了吗，然后就是已保存和同步问题
每次切换进入的文件状态应该是已同步，已保存，只有修改后才会是未保存，然后在500毫秒内进行保存同步

1. 我敢聚本地的错别字实用性不强啊，我给了一段文字，基本检测不出来，反而是正确的地方都被标注错误

1. 风险内容检测，检测结果也需要跳转到对应的位置啊

1. 设置大纲，设定等浮层的初始高度为80vh

1. 我现在需要进行修改，查找替换功能的快捷键作用范围应该是扩大，大纲，设定，人设，素材的文件区域也需要查找替换功能，现在是没有的

1. 小说正文编辑区打开的查找替换区域，进行全书查找替换时，只针对分卷目录
2. 在大纲，设定，人设，素材的浮层文件编辑区 ctrl+f需要可以打开查找面板，然后查找的范围是当前文件，和当前类型的文件夹，例如大纲就是某个大纲文件，或者是整个大纲文件夹下的所有文件

1. 添加相关打印提示，我需要知道你设置的内容到底有没有正确记录，因为现在我在素材的编辑区打不开查找替换面板

1. 你需要注意，我是模块化的，大纲，设定，人设，素材的查找替换是在浮层的编辑区查找定位，而不是正文内容的编辑区
2. 查找替换的面板是需要置顶的