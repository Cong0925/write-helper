1.浮层可以有自己的滚动条，然后是检测结果要都列出来，错别字现在只展示了两个，而且需要点击就可以直接到对应位置将出问题的地方直接高亮选中
2.风险段落改为风险检测
3.敏感词管理的当前词库只显示 20 个，多余的省略号代替，不可展开，然后你这个导入后的模板，直接就是默认使用，不需要有应用按钮
4.我认为当前词库的设计还是不好，加入我有 1000 个， 1 万个敏感词，难道你都通过这个标签显示吗，所以具体的展现形式你得思考转变一下

　　1.每一种检测的标题部分都可以进行点击展开或折叠
2.敏感词部分当前词库你可以仍旧采用原来的标签形式，然后只展示 10 个，多余采用省略号，点击后可以有个弹窗，是一个管理表格，可以根据模板切换表格展示内容，可以根据名称进行模糊查询，你试试这个思路去修改，我的描述可能功能不是很完善，你需要进行完善丰富，记住用户体验优先
3.自定义纠错标题部分添加一个设置词库，这里点击后，直接是一个弹出面板，可拖拽，不影响别处修改，这个面板分左右，左边 1 份，右边 3 份，左边是列表，每一项可以进行开关，右侧是两列可输入内容的表格，第一列检测词，第二列修正词（选填），弹窗标题：自定义纠错词库，左侧最下面是新建按钮，可以新建列表项，新建时可以输入名字，默认点开弹窗时是词库 1 ，新建时，词库名称递增，长度最长 10 个字符

　　1.浮层面板需要设置最大高度 90vh
2.敏感词词库管理你理解错了，我的意思是在上一版本的基础上，添加敏感词，导入模板都不变，只是将中间的敏感词展示的部分仍旧采用初版的小标签，但是只限制展示 10 个，多余的用省略号，点击后再出现现在的这个敏感词的查看面板

　　1.风险监测的设置词库点击后，没有新的面板弹出，现有面板内容变更成从上到下
返回，输入框添加敏感词到默认模板
导入模板按钮，点击后有个文本区域可以粘贴，提示敏感词之间用逗号，分号，顿号，空格分割，可以设置模板名称，点击保存后保存这个敏感词模板
然后是敏感词展示，默认 10 个标签展示，剩余的省略号，点击后出现新的面板，可以根据模板，名称模糊查询铭感词
现在你明白了吗

　　1.敏感词的新面板显示有问题，点击设置词库不展示，点击了敏感词标签也没显示，只有返回了检测才显示，检查相关逻辑，这不对
1.敏感词库管理的显示位置也不对，就在浮层面板的左侧就行了，现在的位置太偏离了

　　1.检查敏感词库的显示位置，还是没有正确定位
1.自定义纠错的检测是按照其设置的自定义纠错词库进行的，检测文章中出现的检测词，然后提示修改成修正词

　　1.新问题，我认为校对这一部分的相关检测字典，敏感词库，自定义纠错词库，具体的内容不应该直接在代码里，而应该是有专门的一个文件夹存放，存放对应的 json 文件，专门进行存储管理，例如我设置了自定义纠错的内容，但是，我刚打开项目时，并没有检测出来，只有我打开词库面板，然后返回了才检测出我设置的内容，这些添加的内容应该本地保存到数据文件，这样不论你是否是直接打开还是一直在使用，检测时只要有内容，就可以检测出。所以需要你完善一下相关目录结构。
2.点击修正后，数量应该变化，当前修正的提示应该在列表去除

　　1.你觉得给检测结果的忽略添加本次忽略可以吗，现在的忽略好像并没有实际功能，点击后只是当前列表去除了，但是点击检测还是被检测出来了，但实际的忽略，其实就是想当前文件的这部分内容我特意写成这样的，检测的时候不想让你再告诉我要修改了，所以，我觉得忽略你可以完善一下，点击后对当前文件内容下次检测就不管了

　　1.字体设置的默认值是
字号 15
行高 1.5
行宽 1000
恢复默认也是按照这个值来
2.切换文件显示后，上次检测的内容列表清空，需要重新检测，即便再次切换回来当前文件也需重新检测

　　1.现在开始对右侧大纲，人设，设定，素材进行开发，首先这些图标点击后对应的文件夹就是当前项目根目录对应的文件夹
2.然后这些图标点击后，会有浮层面板，宽度 550px ，高度 80vh 固定，然后也是可拖拽，不影响别处修改
先按照这个要修修改一下

　　1.现在需要注意的是，右侧区域图标点出的目录文件，点击时，不会在中间区域显示内容，中间区域的内容只会显示主页面左侧菜单的正文内容
2.针对第一点，所以右侧图标对应的文件需要在它当前的这个浮层面板展示出来，我打算分两部分，左侧右侧，左边占 4 份，右侧 1 份，左侧是内容，右侧是对应的目录菜单，可以新建删除，修改名称，刷新

　　1.浮层问题，大纲等是因为宽度增加了，所以左上角定位点偏左，但是校对的宽度不是那么宽，所以它左上角的定位偏左了，需要修正
2.大纲等内容的浮层比例变化一下，左 5 右 2 吧

　　1.浮层的定位再往上 50px
2.人设部分的需要修改右侧部分可以查询，可以建文件夹，可以建文件，新建文件夹叫新建分类，新建文件叫新建角色，每一个文件对应一个角色，然后新建后的内容有固定格式 角色名，背景，性格，外貌，其他，提及章节，按照顺序确定这些标题，然后之间留有空余用户可以填写内容

　　1.浮层修改名称后文件夹、文件消失，可能又是路径错误，检查，然后对文件可以进行转移操作，可以将其移动到当前指定文件夹下
2.分类和角色可以一直点击添加没有限制，可以记录当前点击的是哪个分类，然后新建角色时默认添加到当前分类下，左侧内容区域要是可以进行编辑的，角色名和文件名同步

　　1.人设部分还是不对，右侧的菜单仔细检查功能，确保：
点击分类时能够进行文件夹的新增，名称按照顺序递增，可修改名称，内容为空时可删除
点击角色时能够进行文件的新增，名称按照文件在当前文件夹下的顺序递增，添加时如果点击了某个文件夹就在那个文件夹下新建，
文件可以进行移动，选择不同的分类文件夹进行移动
仔细检查述功能

　　1.浮层的比例变换成左 5 比右 3
2.人设的分类和角色需要按照层级展示，而不是直接点击分类进入文件查看角色，类似于分卷和章节那样展示，可折叠
3.角色移动时，选择完分类就直接返回刷新列表了，不要停留在原地
4.你给分类和角色的添加添加限制了吗，为什么点击一次后再点击就没有效果了
仔细检查上述问题

　　1.人设按钮点击后出现的浮层，添加角色和添加分类，其自增序号是分别计算的，互不影响，然后点击添加角色的按钮功能还是有问题，点击后没有添加角色文件

　　1.没有任何报错，但是点击角色添加就是一直添加不了， 但是通过手动点击我发现的问题，就是你不选择分类目录的时候，点击添加角色可以直接添加，但是点击了某个分类目录后，再点击添加角色就一直添加不了，检查这个问题

　　1.你是不是把所有有关文件目录的都写到一个文件了？你可以分模块啊，卷和章节的处理逻辑单独放一个文件，大纲的单独放，分类和角色的单独放，这样各个目录系统之间不就不会相互影响了，你的整体代码文件也不会很冗余了，你要学会分模块啊

　　1.大纲的目录部分也需要有搜索，根据文件名模糊搜索
2.人设部分对分类或者角色删除时，没有重新加载目录，点击刷新都没用，得切换到别的面板，然后再回来它的目录才显示被删除后的内容，需要检查逻辑

　　1.大纲新建后，或者名称更改后，其对应文件内容第一行显示的文件名需要同步更换
2.添加角色后，其文件内容的角色名就是当前文件名，当文件名修改后，其角色名对应的内容需要同步更改

　　1.关于分类的折叠我感觉需要修改，如果用户需要频繁切换分类创建角色，但是你每次点击就展开折叠，很影响感官，所以，分类的展开折叠只有点击前面的图标按钮才进行，点击分类名称时不进行
2.新添加的分类添加在最后，不要插在中间
3.可以对分类进行上下移动，第一个只能下移动，最后一个只能上移动，文件同理，此时标题后面的按钮过多，所以需要管理，只把重命名显示，然后其余功能折叠到...里面，鼠标悬浮或点击时，展开，显示上移，下移，切换分类，删除

　　1.我是不是设定了目录的顺序排列？这个排列的执行逻辑得更换，只有创建新的文件，文件夹需要在末尾添加，具体的顺序你不负责调整了，由用户自己上移，下移进行调整，但是现在的上下移动并没有将分类，角色进行位置变化，检查完善逻辑
2.切换分类的逻辑也有问题，点击后没反应，需要检查
3.我感觉这个所有的文件目录都可以按照，重命名，...这种形式，多余的标题功能都放到更多里，避免结构混乱
现在开始完善代码

　　1.人设分类默认展开所有分类
2.大纲的上下移也需要有确切的功能
3.角色文件切换分类时，如果目标文件夹已有同名，当前的文件名后添加-迁移，表明是迁移过来的
4.上移下移的判断需要添加，如果当前文件，文件夹处于第一个，最后一个，那么对应的移动需要不可使用
5.分类目录现在点击上移下移没有效果

　　1.大纲点击上下移动后没有效果，是不是原先的默认排序没修改？同时新增时默认名称改为 大纲 1 ， 2 ， 3 这样子的
2.角色文件切换分类时，需要有相应的提示文字，不要把目标文件已存在等报错内容在控制台啊，用户也需要知道没有反应的原因
3.我前面说的重名后添加-迁移不是就只是这样，是在现有名称后添加，例如已经有了角色 1 ，迁移进来的是角色 1-迁移，然后又迁移进来一个角色 1 ，此时就是角色 1-迁移 1 ，灵活变通
4.如果角色切换的是当前分类需要进行提示，当前角色正处于当前分类
5.分类的上下移动还是没有效果，那就把其上下移动按钮取消，分类不能移动，只能添加时默认添加在最后

1.现在大纲，人设，设定，素材目录，其刷新功能不再对文件，目录进行排序，只检查更新，然后只有新增的内容排末尾
2.设定的新增文件名按照设定1，2，3这样子取名
3.素材的新增文件名按照素材1，2，3这样子取名
4.对于大纲，人设，设定，素材目录下的文件，其需要有格式，比如内容的一些小标题需要加黑，加粗，字体放大，所以，不能是简单的的textarea了，面板需要变换

1.大纲，人设，设定，素材目录的刷新按钮还是会对目录进行排序，现在不需要变换顺序
2.大纲，人设，设定，素材文件展示面板部分，不要这两个按钮，我的意思是是否也需要使用什么文本编辑器之类的

1.大纲，人设，设定，素材目录的样式怎么还变了，赶紧优化

1.新建的角色文件排版有默认的，每一项标题都是h3,然后每个标题之间有空行保持间距

1.还是不对，角色名部分没有使用h3标题，然后每个部分之间至少要有2行空白行间距
2.大纲，人设，设定，素材目录的更多按钮，其展现位置也需要定位啊，虽然有滚动条，但是尽可能的还是在当前部分就展示完全，可以变换定位点，展现向上，向下排列，不要被隐藏了

1.还是不对，检查你的writeFile函数
是不是    await writeFile(path, `### 角色名：${charName}\n\n\n\n### 背景：\n\n\n\n### 性格：\n\n\n\n### 外貌：\n\n\n\n### 其他：\n\n\n\n### 提及章节：\n`)
在进行传输的时候回车符被去除了，
2.算了，大纲，人设，设定，素材目录的更多按钮，你在出现的时候判断当前是在浮层的上面四分之三处以内吗，如果是则正常显示，不是的话，那就说明在浮层的下面四分之一高度区域内，那更多按钮的弹出就网上排列

1.你的解决方法还是不行，实在不行对内容进行转换的时候，但 marked 渲染 HTML  时标题间的空行不产生可视间距。你使用margin的方法不行，直接采用将回车换行转换成<div><br></div，有几个回车换行就有几个前面的元素

1.人设部分的文件的更多按钮的展示位置逻辑是不是还是有问题，没有按照预设的在下四分之一高度内向上展开

1.首页的删除需要有提示，避免误删
2.右侧添加图标，可以直接打开其所在文件资源管理器目录
3.删除是物理删除，直接将其所在文件夹目录删除
4.添加刷新列表功能，点击后重新加载项目目录

1.点击文件夹图标后，需要在打开的资源管理器界面跳转到其当前所在目录啊，不要只是打开了资源管理器
2.其实这个拆书和进入项目后的合并导出是搭配的，所以，现在你需要修改一下合并导出的格式，
第一卷 啊啊啊啊
第一章水水水水水
正文

1.拆书的逻辑还要检查，例如 拆的项目名是什么，应该是上传的那个文件的文件名
2.我觉得拆书还是不太好听，直接改名叫导入项目

1.合并导出章节功能需要检查，首先是导出界面的预览就不对怎么还是
<!-- 第1卷 / 第1章 修改记录5-25 -->
应该是
第1卷
第1章 修改记录5-25
才对
然后，导出的文件内容我打开看了，怎么没有第几卷？
仔细检查逻辑
2.导出成功后，不要只是一个确认弹窗，可以增加功能，打开文件所在位置，点击后直接打开文件资源管理器打开其文件位置


1.导入功能还需完善，你不是见到1，23，十一，十二就是章节，要根据前后结合符合 第XX章，第XX卷，这种形式才是分卷，分章，其余的可能是正文内容提到的，所以还需要修改拆分逻辑

1.导入结构还是有问题
一般来说导入的结构是
第一卷 啊啊啊
第一章 杀杀杀
卷号和卷名，章节和章节名是在同一行的，你现在没有加载卷名，和章节名


1.哦我知道了还有一个问题，你导入的逻辑引用了新建项目对吗，那就会在你导入的目录前面也会新增第一卷第一章，其实导入是不需要的，进行修改

1.检查打开已有项目，我现在设置了默认目录，但是如果我打开的项目是默认项目路径以外的项目，那么它就没法正常显示项目结构，检查原因

1.检查我的项目我点击打开所在文件夹，怎么还是之打开了资源管理器，难道是你没拿到项目的路径吗
还是不对无法正常打开跳转，仔细进行检查

1.我知道了，有没有可能传递的是文件夹的目录路径，所以系统不能正确跳转，我看如果穿的是具体的文件路径就可以
